# Kiro 開発ルール テンプレート

## 概要

開発を進めるうえで遵守すべき標準ルールを定義します。
本ファイルは新規プロジェクトのテンプレートとして使用します。

## プロジェクト構造

本リポジトリは、XXXアプリケーション専用のリポジトリです。

### ドキュメントの分類

#### 1. 永続的ドキュメント（`.kiro/steering/`）

アプリケーション全体の「**何を作るか**」「**どう作るか**」を定義する恒久的なステアリングファイル。
アプリケーションの基本設計や方針が変わらない限り更新されません。
全セッションで自動的に読み込まれ、プロジェクトの「北極星」として機能します。

- **product.md** - プロダクト定義
  - プロダクトビジョンと目的
  - ターゲットユーザーと課題・ニーズ
  - 主要な機能一覧
  - 処理フロー
  - 成功の定義

- **tech.md** - 技術仕様
  - テクノロジースタック
  - 開発ツールと手法
  - 環境変数定義
  - 依存関係の方針
  - API呼び出しルール
  - 技術的制約と要件
  - パフォーマンス要件

- **structure.md** - リポジトリ構造定義
  - ソースコードのディレクトリ構成と各ファイルの役割
  - ファイル配置ルール
  - 実行時ディレクトリ構造
  - ドキュメント構成

- **coding-conventions.md** - コーディング規約
  - 言語固有の規約
  - スタイリング規約（フォーマッター/リンター設定）
  - 設計パターン
  - エラーハンドリング方針
  - コメント・ログの言語ルール

- **commands.md** - コマンド実行ルール
  - package.json スクリプト一覧
  - ビルド・テスト・リントのコマンド
  - 直接コマンドを使わない理由

- **process.md** - 開発プロセス（`inclusion: fileMatch`, `fileMatchPattern: ".kiro/specs/**/*"`）
  - ドキュメント管理原則
  - Spec運用フロー
  - 品質チェック手順
  - 図表ルール
  - 永続ドキュメントの更新基準

- **glossary.md**（任意） - ユビキタス言語定義
  - ドメイン用語の定義
  - ビジネス用語の定義
  - UI/UX用語の定義
  - 英語・日本語対応表
  - コード上の命名規則

#### 2. 作業単位のドキュメント（`.kiro/specs/YYMMDD-作業名/`）

特定の開発作業における「**今回何をするか**」を定義する一時的なドキュメント。
作業完了後は参照用として保持されますが、新しい作業では新しいディレクトリを作成します。

- **requirements.md** - 今回の作業の要求内容
  - 変更・追加する機能の説明
  - ユーザーストーリー
  - 受け入れ条件
  - 制約事項

- **design.md** - 変更内容の設計
  - 実装アプローチ
  - 変更するコンポーネント
  - データ構造の変更
  - 影響範囲の分析

- **tasks.md** - タスクリスト
  - 具体的な実装タスク
  - 依存関係
  - タスクの進捗状況
  - 完了条件

### Spec ディレクトリの命名規則

```
.kiro/specs/[YYMMDD]-[作業名]/
```

**例：**
- `.kiro/specs/250103-initial-implementation/`
- `.kiro/specs/250115-add-tag-feature/`
- `.kiro/specs/250120-fix-filter-bug/`
- `.kiro/specs/250201-improve-performance/`

**命名のガイドライン：**
- 日付は作業開始日（6桁: YYMMDD）
- 作業名は英語のケバブケースで簡潔に
- 機能追加: `add-xxx`、バグ修正: `fix-xxx`、改善: `improve-xxx`、リファクタ: `refactor-xxx`

## 開発プロセス

### 初回セットアップ時の手順

#### 1. ステアリングファイル作成（`.kiro/steering/`）

アプリケーション全体の設計を定義します。
各ドキュメントを作成後、必ず確認・承認を得てから次に進みます。

1. `.kiro/steering/product.md` - プロダクト定義
2. `.kiro/steering/tech.md` - 技術仕様
3. `.kiro/steering/structure.md` - リポジトリ構造定義
4. `.kiro/steering/coding-conventions.md` - コーディング規約
5. `.kiro/steering/commands.md` - コマンド実行ルール
6. `.kiro/steering/process.md` - 開発プロセス
7. `.kiro/steering/glossary.md` - ユビキタス言語定義（任意）

**重要：** 1ファイルごとに作成後、必ず確認・承認を得てから次のファイル作成を行う

#### 2. 初回実装用の Spec 作成

初回実装用のディレクトリを作成し、実装に必要なドキュメントを配置します。

作成するドキュメント：
1. `.kiro/specs/YYMMDD-initial-implementation/requirements.md` - 初回実装の要求
2. `.kiro/specs/YYMMDD-initial-implementation/design.md` - 実装設計
3. `.kiro/specs/YYMMDD-initial-implementation/tasks.md` - 実装タスク

#### 3. 環境セットアップ

#### 4. 実装開始

`.kiro/specs/YYMMDD-initial-implementation/tasks.md` に基づいて実装を進めます。

#### 5. 品質チェック

### 機能追加・修正時の手順

#### 1. 影響分析

- 永続的ドキュメント（`.kiro/steering/`）への影響を確認
- 変更が基本設計に影響する場合は `steering/` を更新

#### 2. Spec ディレクトリ作成

新しい作業用のディレクトリを作成します。

**例：**
```
.kiro/specs/250115-add-tag-feature/
```

#### 3. 作業ドキュメント作成

作業単位のドキュメントを作成します。
各ドキュメント作成後、必ず確認・承認を得てから次に進みます。

1. `.kiro/specs/YYMMDD-作業名/requirements.md` - 要求内容
2. `.kiro/specs/YYMMDD-作業名/design.md` - 設計
3. `.kiro/specs/YYMMDD-作業名/tasks.md` - タスクリスト

**重要：** 1ファイルごとに作成後、必ず確認・承認を得てから次のファイル作成を行う

#### 4. 永続的ドキュメント更新（必要な場合のみ）

変更が基本設計に影響する場合、該当する `.kiro/steering/` 内のドキュメントを更新します。

**更新基準：**
- 新しいモジュールを追加した → `structure.md` を更新
- 新しい依存ライブラリを導入した → `tech.md` を更新
- 新しい設計パターンやルールを採用した → `coding-conventions.md` を更新
- package.json にスクリプトを追加した → `commands.md` を更新

#### 5. 実装開始

`.kiro/specs/YYMMDD-作業名/tasks.md` に基づいて実装を進めます。

#### 6. 品質チェック

## ドキュメント管理の原則

### 永続的ドキュメント（`.kiro/steering/`）

- アプリケーションの基本設計を記述
- 頻繁に更新されない
- 大きな設計変更時のみ更新
- プロジェクト全体の「北極星」として機能
- 全セッションで自動的に読み込まれる（`process.md` のみ Spec 作業時のみ）

### 作業単位のドキュメント（`.kiro/specs/`）

- 特定の作業・変更に特化
- 作業ごとに新しいディレクトリを作成
- 作業完了後は履歴として保持
- 変更の意図と経緯を記録
- 過去の Spec は将来の類似作業の参考になる

## 図表・ダイアグラムの記載ルール

### 記載場所

設計図やダイアグラムは、関連するドキュメント内に直接記載します。
独立した diagrams フォルダは作成せず、手間を最小限に抑えます。

**配置例：**
- ER図、データモデル図 → Spec の `design.md` 内に記載
- システム構成図 → `steering/structure.md` または Spec の `design.md` 内に記載
- 画面遷移図、ワイヤフレーム → Spec の `design.md` 内に記載

### 記述形式

1. **Mermaid記法（推奨）**
   - Markdownに直接埋め込める
   - バージョン管理が容易
   - ツール不要で編集可能

3. **ASCII アート**
   - シンプルな図表に使用
   - テキストエディタで編集可能

5. **画像ファイル（必要な場合のみ）**
   - 複雑なワイヤフレームやモックアップ
   - `docs/images/` フォルダに配置
   - PNG または SVG 形式を推奨

### 図表の更新

- 設計変更時は対応する図表も同時に更新
- 図表とコードの乖離を防ぐ
- 図表は必要最小限に留め、メンテナンスコストを抑える

## 注意事項

- ドキュメントの作成・更新は段階的に行い、各段階で承認を得る
- Spec のディレクトリ名は日付と作業名で明確に識別できるようにする
- 永続的ドキュメントと作業単位のドキュメントを混同しない
- コード変更後は必ずリント・型チェックを実施する
- セキュリティを考慮したコーディング（入力バリデーション、適切なエラーハンドリング）


