# Invoking as sub-agents

> 元URL: https://kiro.dev/docs/custom-agents/subagents/  
> 最終取り込み日: 2026-09-05  
> このページは自動翻訳（英語→日本語）です。コード部分は翻訳していません。

---

サブエージェントを使用すると、特定のタスクを、独自の隔離されたコンテキストで実行されるエージェントに委譲することができます。メインエージェントは、タスクの並列処理、専用ツール、またはコンテキストの隔離が有効な場合にサブエージェントを生成し、処理が完了すると結果を集約します。

| 機能 | IDE | CLI | Web | モバイル |
| --- | --- | --- | --- | --- |
| サブエージェントの自動起動 | ✓ | ✓ | ✓ | ✓ |
| サブエージェントの明示的な呼び出し | ✓ | ✓ | ✓ | ✓ |
| サブエージェントとしてのカスタムエージェント | ✓ | ✓ | ✓ | — |
| 並列実行 | ✓ | ✓ | ✓ | ✓ |

Web版では、`.kiro/agents/`にコミットされたカスタムエージェントを、Kiroの[組み込みサブエージェント](https://kiro.dev/docs/custom-agents/subagents/#built-in-sub-agents)と同様にサブエージェントとして起動できます。モバイル版では、組み込みサブエージェントのみが使用されます。

## サブエージェントの仕組み

1. **タスクの割り当て** - タスクを記述します（あるいは、メインエージェントが委任によってメリットが得られるタスクを特定します）。
2. **エージェントの選択** - Kiroは、タスクと利用可能なエージェントに基づいて、適切なエージェント構成を選択します
3. **隔離された実行** - サブエージェントは、独自のコンテキストウィンドウ、ツール、および権限を持って実行されます
4. **結果の返却** - 処理が完了すると、サブエージェントは結果をメインエージェントに返します

任意の[カスタムエージェントを](https://kiro.dev/docs/custom-agents/)サブエージェントとして呼び出すことができます。メインエージェントは、エージェントの ``description`` フィールドに基づいて自動的にエージェントを選択しますが、明示的にエージェントを指定することも可能です：

```text
Use the code-reviewer agent to analyze src/auth/ for security issues
```

## サブエージェントが継承するもの

サブエージェントはワークスペース環境を共有しますが、会話コンテキストは分離された状態で実行されます：

| メインエージェントと共有されるもの | サブエージェントごとに分離 |
| --- | --- |
| [ステアリングファイル](https://kiro.dev/docs/steering/) | 会話履歴 |
| [MCPサーバー](https://kiro.dev/docs/mcp/) | コンテキストウィンドウ |
| ワークスペースファイルへのアクセス | [仕様](https://kiro.dev/docs/specs/)状態 |
| [権限](https://kiro.dev/docs/permissions/)設定 | [フックの](https://kiro.dev/docs/hooks/)トリガー |

## 組み込みサブエージェント

Kiroには、適切な場合に自動的に使用される2つの内部サブエージェントが含まれています：

- **コンテキスト収集** - プロジェクト構造を調査し、関連するファイルやコンテキストを収集します
- **汎用** - デフォルトのエージェント設定を使用して、あらゆる種類の並列処理タスクを処理します

これらを設定する必要はありません。メインエージェントが必要に応じてこれらを使用します。

## 並列実行

サブエージェントは並列で実行され、それぞれが独立して動作します。これは次のような場合に役立ちます：

- 複数のファイルを同時にリファクタリングする場合
- 複数のトピックを並行して調査する
- 独立した分析トラックの実行

```text
Refactor these three services to use the new auth middleware - do them in parallel
```

## タスクの依存関係

サブエージェントは、タスク同士が相互に依存する有向非巡回グラフ（DAG）をサポートしています。メインエージェントは、タスクグラフ全体を事前に計画し、適切な順序で実行します。具体的には、独立したタスクを並行して実行し、依存関係があるタスクについては、依存するタスクが完了するのを待ってから開始します。

```text
  ┌─────────────┐
  │  1. Analyze  │
  │ dependencies │
  └──────┬───────┘
         │
  ┌──────▼───────┐
  │ 2. Refactor  │
  │   modules    │
  └──────┬───────┘
         │
  ┌──────▼───────┐
  │  3. Run and  │
  │  fix tests   │
  └──────────────┘
```

## レビューのループ

あるステージは、その出力結果からさらなる作業が必要であると判断された場合、前のステージに戻ることができます。例えば、レビュー担当者が修正のために作業を実装担当者に差し戻す場合などです。

```text
Implement the feature, then have a reviewer check it and send it back for fixes until it passes
```

エージェントは、以下の要素を用いてループするパイプラインを構築します：

- **ターゲット** — ループがトリガーされた際に再実行するステージ
- **トリガー** — ループを起動するステージ出力内のテキスト（例：`NEEDS_CHANGES`）
- **max_iterations** — 安全上限（1～10の間）

制約：

- ステージは自分自身へループバックすることはできません
- 相互ループ（A → B → A）は許可されません
- トリガーは4文字以上でなければならない

## ツールの利用可能性

デフォルトのサブエージェントには、メインエージェントと同じ組み込みツール（`read`、`write`、`shell`、`web_search`、`web_fetch`、および設定済みのMCPツール）が備わっています。

カスタムエージェントに権限を委譲する場合、サブエージェントはそのエージェントの`tools`および`permissions`の設定を使用します。カスタムエージェントの設定にリストされていないツールは、サブエージェントでは利用できません。

## サブエージェントへのアクセス設定

[エージェント設定](https://kiro.dev/docs/custom-agents/configuration-reference/)内の ``toolsSettings.subagent`` を使用して、生成可能なエージェントと、承認プロンプトなしで実行されるエージェントを制御します。

json

```json
{
  "toolsSettings": {
    "subagent": {
      "availableAgents": ["reviewer", "tester", "docs-*"],
      "trustedAgents": ["reviewer", "tester"]
    }
  }
}
```

| フィールド | 説明 |
| --- | --- |
| `availableAgents` | このエージェントが起動できるエージェントのグロブパターン。省略するとすべて許可されます。 |
| `trustedAgents` | 承認プロンプトなしで実行されるエージェント。 |

サブエージェントが使用できるツールを制限するには、親エージェントではなく、**サブエージェント自身の設定ファイル**で ``tools`` を設定してください。

## サーフェス固有の挙動

サブエージェントは並行して実行され、メインエージェントはすべてが完了するまで待機してから処理を続行します。各サブエージェントには独自のコンテキストウィンドウがあります。

サブエージェントを活用して複数のタスクを同時に実行することで、開発をスピードアップできます。たとえば、複数のチケットを並行して取得・分析するなどです。

[](/videos/subagents.mp4?h=d538efc3)

## ベストプラクティス

1. **複雑なタスクに使用** - 分離や並列処理の恩恵を受ける、複数のステップからなる操作に最も有効
2. **明確な指示を与える** — 具体的なタスクの説明により、適切なエージェントの選択と良好な結果が得られます
3. **進捗状況を監視する** — CLI では、Ctrl+G を使用して、実行に時間がかかるサブエージェントの状況を確認します
4. **アクセスの設定** — 多数のサブエージェントを生成するオーケストレーターエージェントには、`availableAgents` および `trustedAgents` を使用してください

## トラブルシューティング

| 問題 | 解決策 |
| --- | --- |
| サブエージェントが起動しない | タスクの説明が明確で、実行可能であることを確認してください |
| ツールへのアクセス権限がない | エージェント設定の「`tools`」フィールドを確認してください |
| 承認プロンプトによるブロック | `trustedAgents`にエージェントを追加するか、`subagent`ツールを信頼するように設定してください |
| メインエージェントがサブエージェントを起動できない | オーケストレーターエージェントの ``tools`` 配列に ``subagent`` を追加してください |
| 結果が不完全 | より具体的な指示を与えるか、タスクをさらに細分化してください |


---

[← 前へ: Configuration reference](configuration-reference.md) | [↑ 親ページ](index.md) | [次へ →: Examples](examples.md)
