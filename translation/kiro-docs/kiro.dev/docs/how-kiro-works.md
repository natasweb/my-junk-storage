# How Kiro works

> 元URL: https://kiro.dev/docs/how-kiro-works/  
> 最終取り込み日: 2026-09-05  
> このページは自動翻訳（英語→日本語）です。コード部分は翻訳していません。

---

Kiroは単一のエージェントであり、仕事をする場所ならどこでも利用可能です。ノートパソコンを閉じても、セッションはクラウド上で引き続き実行されます。 スマートフォンから状況を確認し、翌朝にはIDEで作業を再開できます。IDE、CLI、Web、モバイルアプリは、すべて同じ**統合エージェント・ハーネス**と通信するための異なる手段に過ぎません。したがって、ステアリングルールを設定したり、カスタムエージェントを作成したり、MCPサーバーを接続したりする際は、特定のアプリではなく、Kiroそのものを設定していることになります。

この一つの考え方が、他のすべてを整理しています。機能はKiroレベルに存在し、「**機能（Features）**」で一度だけ文書化され、各「インターフェース」セクションでは、そのインターフェースが追加する機能のみを扱います。このページでは、各要素がどのように組み合わさっているかを解説します。このアーキテクチャの背景にある技術的なストーリーについては、Kiroブログの[「One agent, every surface」](https://kiro.dev/blog/one-agent/)をご覧ください。

## 1つのハーネス、多数のサーフェス

その中心にあるのが、統一されたエージェント・ハーネスです。これは、エージェントの実行に関わるすべて（会話のオーケストレーション、ツールの実行、コンテキストの管理、権限の評価、LLMプロバイダーとの通信）を処理します。すべてのKiroサーフェス（IDE、CLI、Web、モバイル）は、この同じハーネスのフロントエンドです。

このハーネスは、各アプリにコンパイルされたライブラリではなく、独立したプロセスです。コードベースと並行して実行され、エージェント側のすべてを管理し、クライアント側はそれとのインタラクション方法を管理します。独立したプロセスであるため、同じハーネスをノートパソコン上でもクラウドのサンドボックス内でも起動でき、どちらでも全く同じように動作します。

ハーネスが共有されているため、機能は利用可能な場所を問わず同じように動作します。権限ルールは、CLIでもIDEでも同じ操作を拒否します。ステアリングファイルは、すべてのインターフェースでエージェントの挙動を同一に制御します。コンパクションは同じ情報を保持します。 各インターフェースの違いは、エージェントを操作する方法にあり、エージェントが実行できる機能そのものにはありません。ある機能がまだ特定のインターフェースに実装されていない場合、その機能ページの利用可能状況表に明記されています。

### 境界となるのはプロトコルです

クライアントは、オープンな[エージェントクライアントプロトコル](https://kiro.dev/docs/cli/acp/)（ACP）を通じてハーネスと通信します。 これにはKiro独自のインターフェースも含まれます。IDE、CLI、Web、モバイルアプリはすべてACPを使用しており、ライブステアリング、仕様ワークフロー、リッチな権限プロンプト、コンテキスト使用状況の追跡などの機能のために、Kiro固有のメソッド（仕様の`_kiro/`名前空間下）で拡張されています。 ローカルクライアントは stdio 経由で接続し、Web およびモバイルは WebSocket 経由でサンドボックス化されたハーネスに接続します。どちらのトランスポートでも、同じバイナリ、同じツール、同じ動作が実現されます。

## ターン（操作）の仕組み

どのインターフェースからプロンプトを送信しても、ハーネスは同じループを実行します：

1. **コンテキストが構築されます。**ハーネスは、プロンプト、これまでの会話履歴、[ステアリング](https://kiro.dev/docs/steering/)ファイル、および添付されたすべての情報を収集します。ここで[プロンプト送信フックが](https://kiro.dev/docs/hooks/types/#prompt-submit)実行され、独自のコンテキストを注入することができます。
2. **モデルが計画を立てます。**リクエストは[選択したモデルに](https://kiro.dev/docs/models/)送信されるか、Autoがタスクに最適なモデルへルーティングし、テキスト、ツール呼び出し、またはその両方が返されます。
3. **すべてのツール呼び出しがチェックされます。**ツールが実行される前に、[権限ルール](https://kiro.dev/docs/permissions/)（「deny」は「ask」より優先され、「ask」は「allow」より優先される）が評価され、有効になっている場合は[kiroignore](https://kiro.dev/docs/kiroignore/)パターンも評価されます。[「Pre Tool Use」フックは、](https://kiro.dev/docs/hooks/types/#pre-tool-use)呼び出しを許可またはブロックすることができます。
4. **ツールが実行されます。**[組み込みツールは](https://kiro.dev/docs/tools/)、ファイルの読み取りや編集、シェルコマンドの実行、Web検索を行います。[MCPサーバーや](https://kiro.dev/docs/mcp/)[Powers](https://kiro.dev/docs/powers/)[が](https://kiro.dev/docs/mcp/)機能セットを拡張し、[サブエージェントが](https://kiro.dev/docs/custom-agents/subagents/)委任された作業を引き受けます。[「Post Tool Use」フックは](https://kiro.dev/docs/hooks/types/#post-tool-use)結果に応じて反応します。
5. **結果がフィードバックされます。**ツールの出力はモデルに戻され、タスクが完了するまでこのループが繰り返されます。ターンが完了すると、[エージェント停止フック](https://kiro.dev/docs/hooks/types/#agent-stop)が発火します。
6. **コンテキストは管理された状態を維持します。**会話がモデルのコンテキスト制限に近づくと、[コンパクション機能によって](https://kiro.dev/docs/compaction/)古い履歴が要約され、セッションが継続されます。

このループこそが、「機能」セクションが現在の構成になっている理由です。各機能はループの段階であり、単一のアプリに後付けされたものではありません。一度記述した権限ルールは、あらゆる場所でのステップ3を制御します。ステアリングファイルは、あらゆる場所でのステップ1を形作ります。これが「単一のハーネス」という表現の実用的な意味です。

## ハーネスが管理するもの

各ハーネスの機能は、Kiroレベルで一度だけ文書化されます：

| 領域 | 対象範囲 |
| --- | --- |
| [仕様](https://kiro.dev/docs/specs/) | 構造化された開発ワークフロー：要件、設計、タスク |
| [運営](https://kiro.dev/docs/steering/) | 一貫したプロジェクトのコンテキストと規約 |
| [フック](https://kiro.dev/docs/hooks/) | エージェントループを軸としたイベント駆動型自動化 |
| [MCP](https://kiro.dev/docs/mcp/) | 外部ツールサーバー、OAuth、ツール検索 |
| [権限](https://kiro.dev/docs/permissions/) | エージェントが実行できる操作に関する機能ベースのルール |
| [カスタムエージェント](https://kiro.dev/docs/custom-agents/) | エージェントプロファイル、組み込みエージェント、サブエージェントへの権限委譲 |
| [エージェントのスキル](https://kiro.dev/docs/skills/) | ポータブルな指示パッケージ（オープンスタンダード） |
| [権限](https://kiro.dev/docs/powers/) | 知識がバンドルされたMCPサーバー、オンデマンドで読み込み可能 |
| [クラウドセッション](https://kiro.dev/docs/cloud-sessions/) | 管理されたクラウドサンドボックスでセッションを実行し、あらゆる端末から接続可能 |
| [コンパクション](https://kiro.dev/docs/compaction/) | 長時間のセッションに対するコンテキストの自動要約 |
| [Kiroignore](https://kiro.dev/docs/kiroignore/) | gitignore スタイルのパターンを使用して、ファイルをエージェントのアクセス対象から除外 |
| [チェックポイントと巻き戻し](https://kiro.dev/docs/checkpoints/) | エージェントによる変更を元に戻す、または以前のターンで会話を分岐させる |
| [組み込みツール](https://kiro.dev/docs/tools/) | エージェントのファイル、シェル、Web、およびコード用ツール |
| [モデル](https://kiro.dev/docs/models/) | モデルカタログ、自動ルーティング、推論の労力 |

これらはサイドバーの「**機能**」セクションにあります（「モデル」には専用のセクションがあります）。すべての機能がすべてのインターフェースで利用可能になったわけではないため、各ページには、現在どこで機能するかを正確に示す利用可能状況表が表示されます。今後の方向性は、あらゆる場所で単一のハネスを実現することです。

## エージェントの実行環境

ループ自体はどこでも同じですが、実行されるコンピュータは環境によって異なります：

- **IDEとCLIでは、**デフォルトでユーザーの本機上でハネスが実行されます。ツールはローカルファイルシステム上で動作し、シェルコマンドはユーザーのシェルで実行され、ビルドにはローカルにインストールされたツールチェーンと認証情報が使用されます。また、これらはいずれも、Webが使用するのと同じ管理されたサンドボックス内で実行される[クラウドセッション](https://kiro.dev/docs/cloud-sessions/)を作成・接続することも可能です。
- **Webとモバイルは、**管理された[クラウドサンドボックス](https://kiro.dev/docs/web/sandbox/)内で同じハーネスを実行します。Kiroは接続されたリポジトリを隔離された環境にクローンし、そこで作業を行い、変更内容をプルリクエストとして返します。お使いのマシン上では何も実行されず、ノートパソコンを閉じた後もセッションは実行され続けます。 モバイルも同様のクラウドセッションを利用するため、ブラウザで作業を開始し、スマートフォンからチェックインを行うことができます。

クラウドセッションは特定のアプリではなく、あなたのアカウントに紐づいているため、ある端末で開始した作業を別の端末で引き継ぐことができます。各端末でのセッションの作成方法および接続方法については、「[クラウドセッション」](https://kiro.dev/docs/cloud-sessions/)を参照してください。

同じワークフロー、同じルール、同じ設定形式です。違いは、作業が行われる場所と、結果がどのようにユーザーに届くかという点だけです。

## 各デバイスによる追加機能

各デバイスセクションでは、エージェントに関する体験、つまりエージェントの起動方法、動作状況の確認方法、および制御を維持する方法について解説しています。また、デバイスは単なる薄いラッパーでもありません。 クライアントは、プラットフォームの方が処理を効率的に行える場合、ハーネスの組み込み機能の代わりに独自のツールを提供できます。例えば、IDEはファイル操作にエディタ独自のAPIを使用し、その上にエディタ固有のコード解析機能を追加しています。

| インターフェース | 何が追加されるか |
| --- | --- |
| [IDE](https://kiro.dev/docs/ide/) | エディタの統合：インライン差分表示、チェックポイント UI、仕様パネル、ドッキング可能なチャット、および #Problems コンテキストを介したエージェントとのエディタ診断情報の共有 |
| [CLI](https://kiro.dev/docs/cli/) | ターミナルネイティブのワークフロー：TUI、スクリプトやCI向けのヘッドレスモード、オートコンプリート、シェルからのセッション管理 |
| [Web](https://kiro.dev/docs/web/) | ブラウザ上のセットアップ不要のエージェント：サンドボックス化された実行、リポジトリへの接続、プルリクエストベースのデリバリー、スケジュールに基づく自動化 |
| [モバイル](https://kiro.dev/docs/mobile/) | 外出先でもKiro：スマートフォンからセッションを開始・操作 |

「概要」セクションを読んでいて、「でも、その機能自体はどのように動作するのか？」と疑問に思った場合は、その答えは一つ上の階層にある「機能」セクションに記載されています。

## 設定はいつでも持ち運べます

設定も同様の共有モデルに従います。一度定義した内容は、Harnessが実行されるあらゆる場所で適用されます：

| スコープ | 場所 | 移動先 |
| --- | --- | --- |
| プロジェクト | `.kiro/` リポジトリ内 | リポジトリ。チームメンバーや、それを開くすべてのインターフェースは、同一のステアリング、仕様、エージェント、フック、およびMCPサーバーを受け取ります |
| ユーザー | `~/.kiro/` あなたのマシン上の | あなた。すべてのローカルプロジェクトにまたがる個人用エージェント、スキル、ステアリング、および設定 |
| ワークスペースの信頼 | `~/.kiro/workspace-roots/<hash>/` | お使いのマシン限定。プロジェクトごとの権限ルールはリポジトリ外に保存されるため、クローンされたリポジトリが自らに信頼を付与することは決してありません |

これが、Kiro WebおよびMobileが設定なしでプロジェクトのエージェントやステアリングを自動的に認識できる理由です。これらは、IDEやCLIがローカルで使用するのと同じ、リポジトリ内の`.kiro/`ディレクトリを読み取ります。詳細については、「[構成スコープ」](https://kiro.dev/docs/configuration/)を参照してください。

## 実用上の意味

- **一度学べば、どこでも使える。**CLI用に記述した`permissions.yaml`は、IDEが参照するファイルと同じです。プロジェクトに追加した機能は、そのプロジェクトを開くすべての環境で動作します。
- **一貫したアップグレード。**Harness の改善点は一箇所に反映され、クライアントプロトコルに影響しない変更は、アプリの更新を必要とせずにすべての環境に適用されます。「`~/.kiro/hooks/`」内のグローバルフックやコンパクション機能の改善も、この方法で提供され、すべての環境に同時に反映されました。
- **チーム全体での一貫性。**リポジトリに ``.kiro/`` をコミットすることで、IDE、ターミナル、ブラウザのいずれを使用する場合でも、全員が同じエージェントの挙動を得ることができます。

## 今後の予定

- 各設定の配置場所や競合の解決方法を定義する[設定スコープ](https://kiro.dev/docs/configuration/)
- エージェントのツール、権限、プロンプトをカスタマイズする[カスタムエージェント](https://kiro.dev/docs/custom-agents/)
- エージェントの動作を制御する[権限](https://kiro.dev/docs/permissions/)
- お好みのデバイスに Kiro をインストールするための[入門ガイド](https://kiro.dev/docs/getting-started/installation/)


---

[← 前へ: Reasoning effort](/docs/models/effort/) | [↑ 親ページ](../../../index.md) | [次へ →: Specs](specs/index.md)
