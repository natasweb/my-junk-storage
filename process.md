---
inclusion: fileMatch
fileMatchPattern: ".kiro/specs/**/*"
---

# 開発プロセス

## ドキュメントの2層構造

### 1. 永続的ドキュメント（`.kiro/steering/`）

プロジェクト全体の「何を作るか」「どう作るか」を定義する恒久的なルール。
基本設計や方針が変わらない限り更新しない。全セッションで自動的に読み込まれ、プロジェクトの「北極星」として機能する。

| ファイル | 役割 | 記載内容 |
|---------|------|---------|
| `product.md` | プロダクト定義 | ツールの目的・ターゲットユーザー・主要機能一覧・処理フロー・関連ドキュメントへのリンク |
| `tech.md` | 技術方針 | 言語・ランタイム・パッケージマネージャー・主要ライブラリ・環境変数定義・依存関係追加の方針・API呼び出しルール |
| `structure.md` | 構造定義 | ソースコードのディレクトリ構成と各ファイルの役割・ファイル配置ルール・実行時ディレクトリ構造・ドキュメント構成 |
| `coding-conventions.md` | コーディング規約 | TypeScript記法・Biomeスタイル設定・設計パターン・エラーハンドリング方針・コメント/ログの言語ルール |
| `commands.md` | コマンド実行ルール | package.jsonスクリプト一覧・直接コマンドを使わない理由 |
| `process.md` | 開発プロセス（本ファイル） | ドキュメント管理原則・Spec運用フロー・品質チェック手順・図表ルール |

### 2. 作業単位のドキュメント（`.kiro/specs/YYMMDD-作業名/`）

特定の開発作業における「今回何をするか」を定義する一時的なドキュメント。
作業完了後は参照用として保持し、新しい作業では新しいディレクトリを作成する。

| ファイル | 役割 | 記載内容 |
|---------|------|---------|
| `requirements.md` | 要求定義 | 変更・追加する機能の説明・ユーザーストーリー・受け入れ条件・制約事項 |
| `design.md` | 設計 | 実装アプローチ・変更するコンポーネント・データ構造の変更・影響範囲の分析・シーケンス図 |
| `tasks.md` | タスク管理 | 具体的な実装タスク・依存関係・進捗状況・完了条件 |

## Spec ディレクトリの命名規則

```
.kiro/specs/[YYMMDD]-[作業名]/
```

**例：**
- `.kiro/specs/250103-initial-implementation/`
- `.kiro/specs/250115-add-tag-feature/`
- `.kiro/specs/250120-fix-filter-bug/`
- `.kiro/specs/250201-improve-performance/`

**命名のガイドライン：**
- 日付は作業開始日（6桁: YYMMDD）
- 作業名は英語のケバブケースで簡潔に
- 機能追加: `add-xxx`、バグ修正: `fix-xxx`、改善: `improve-xxx`、リファクタ: `refactor-xxx`

## 開発ワークフロー

### 新規機能・修正時の手順

1. **影響分析** — 永続的ドキュメント（`steering/`）への影響を確認
2. **Spec ディレクトリ作成** — `.kiro/specs/YYMMDD-作業名/` を作成
3. **requirements.md 作成** — 要求内容を定義 → 確認・承認
4. **design.md 作成** — 設計を定義 → 確認・承認
5. **tasks.md 作成** — タスクを定義 → 確認・承認
6. **実装** — `tasks.md` に基づいて実装を進める
7. **品質チェック** — リント・型チェック実施
8. **永続ドキュメント更新**（必要な場合のみ） — 基本設計に影響する変更があれば `steering/` を更新

**重要：** 各ドキュメントは1つずつ作成し、確認・承認を得てから次に進む。

### 永続ドキュメントの更新基準

以下の場合に `steering/` のファイルを更新する：
- 新しいモジュールを追加した → `structure.md` を更新
- 新しい依存ライブラリを導入した → `tech.md` を更新
- 新しい設計パターンやルールを採用した → `coding-conventions.md` を更新
- package.json にスクリプトを追加した → `commands.md` を更新

## ドキュメント管理の原則

### 永続的ドキュメント（`steering/`）

- プロジェクト全体の「北極星」として機能する
- 頻繁に更新しない（大きな設計変更時のみ）
- 全セッションで自動的に読み込まれる
- 内容は簡潔に保ち、トークン消費を抑える

### 作業単位のドキュメント（`specs/`）

- 特定の作業・変更に特化する
- 作業ごとに新しいディレクトリを作成する
- 作業完了後は履歴として保持する
- 変更の意図と経緯を記録する
- 過去のSpecは将来の類似作業の参考になる

## 図表・ダイアグラムの記載ルール

### 記述形式

1. **Mermaid記法（推奨）** — Markdownに埋め込み可能、バージョン管理が容易
2. **ASCII アート** — シンプルな図表に使用
3. **画像ファイル（必要な場合のみ）** — 複雑なワイヤフレーム等、`docs/images/` に配置

### 配置場所

- 設計図やダイアグラムは関連するドキュメント内に直接記載する
- 独立した diagrams フォルダは作成しない
- 図表は必要最小限に留め、メンテナンスコストを抑える
- 設計変更時は対応する図表も同時に更新する

## 品質チェック

- コード変更後は必ず `pnpm run check` を実施する
- 型チェックは `pnpm run typecheck` で確認する
- セキュリティを考慮したコーディング（入力バリデーション、適切なエラーハンドリング）
- 1ドキュメントごとに確認・承認を得るフローを守る
